#include<iostream> //It tells the compiler to include the input/output stream library before actual compilation
using namespace std;// allows yout to use names from the standard namespace without writing std:: every time

// This is the starting point of execution
int main(){
    cout<<"Pooja"<<endl<<"Bhambid";
    return 0;
}